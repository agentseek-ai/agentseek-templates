# Optional public LangGraph URL. Leave blank for the local backend default.
# Never put provider credentials in VITE_* variables.
VITE_LANGGRAPH_API_URL=http://127.0.0.1:{{ cookiecutter.langgraph_port }}
